---
- name: restart fleet manager
  ansible.builtin.command: systemctl --user restart fleet-manager.service
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ ansible_facts.user_uid }}"
  changed_when: false
