// ═══════════════════════════════════════════════════════════════════════════
// Forge managed instance
// Rendered by skills/fleet-manager/scripts/provision.sh
// ═══════════════════════════════════════════════════════════════════════════
{
  secrets: {
    providers: {
      local: {
        source: "file",
        path: "/home/${USER}/.openclaw/secrets.json",
        mode: "json",
      },
      env: {
        source: "env",
      },
    },
  },

  gateway: {
    mode: "local",
    port: ${PORT},
    bind: "loopback",
    auth: {
      mode: "token",
      token: { "$secret": "local:GATEWAY_AUTH_TOKEN" },
    },
  },

  agents: {
    defaults: {
      workspace: "${WORKSPACE}",
      userTimezone: "America/New_York",

      models: {
        "anthropic/claude-opus-4-5": { alias: "opus" },
        "anthropic/claude-sonnet-4": { alias: "sonnet" },
        "openai/gpt-5.3": { alias: "gpt" },
      },

      model: {
        primary: "anthropic/claude-opus-4-5",
        fallbacks: ["openai/gpt-5.3"],
      },

      thinkingDefault: "adaptive",
      maxConcurrent: 4,
      timeoutSeconds: 900,
    },

    list: [
      {
        id: "main",
        default: true,
        name: "Forge ${INSTANCE_NAME}",
        workspace: "${WORKSPACE}",
        model: "anthropic/claude-opus-4-5",
        params: {
          thinkingDefault: "adaptive",
        },
        subagents: { allowAgents: ["*"] },
      },
    ],
  },

  bindings: [
    // Add per-instance bindings after provisioning.
  ],

  channels: {
    signal: {
      enabled: false,
      dmPolicy: "allowlist",
      allowFrom: [],
    },
  },

  tools: {
    profile: "coding",
    sessions: { visibility: "tree" },
    exec: {
      security: "full",
      ask: "off",
      backgroundMs: 15000,
      timeoutSec: 3600,
      notifyOnExit: true,
    },
    elevated: {
      enabled: true,
    },
    subagents: {
      tools: {
        deny: ["gateway", "cron", "browser", "canvas", "nodes", "message"],
      },
    },
  },

  cron: {
    enabled: true,
    maxConcurrentRuns: 1,
    sessionRetention: "48h",
  },

  acp: {
    enabled: true,
    backend: "acpx",
    defaultAgent: "codex",
    allowedAgents: ["codex", "claude", "pi", "opencode", "gemini"],
    maxConcurrentSessions: 4,
    runtime: {
      ttlMinutes: 120,
    },
  },

  skills: {
    entries: {
      "coding-agent": { enabled: true },
      github: { enabled: true },
    },
  },

  logging: {
    level: "info",
    consoleLevel: "info",
    consoleStyle: "pretty",
  },
}
